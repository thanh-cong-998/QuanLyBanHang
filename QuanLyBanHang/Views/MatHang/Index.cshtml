@model IEnumerable<QuanLyBanHang.Models.MatHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenMatHang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LoaiMatHang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HangSX)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgaySX)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GiaBan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenMatHang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LoaiMatHang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HangSX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgaySX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaBan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaMatHang }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaMatHang }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaMatHang })
        </td>
    </tr>
}

</table>
