@model IEnumerable<QuanLyBanHang.Models.ChiTietDonHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Chi tiết đơn hàng</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhieuDonHang.ThanhTienID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenMatHang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoLuong)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TongTien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayMuaHang)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhieuDonHang.ThanhTienID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenMatHang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TongTien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayMuaHang)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CTDH_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CTDH_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CTDH_ID })
        </td>
    </tr>
}

</table>
